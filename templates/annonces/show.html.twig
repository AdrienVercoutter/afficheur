{% extends 'base.html.twig' %}

{% block title %}Images{% endblock %}

{% block body %}
    <h1>Images</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ annonce.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ annonce.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ annonce.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="path('app_annonces_index')">Retour a l'acceuil</a>

    <a href="{{ path('app_annonces_edit', {'id': annonce.id}) }}">Editer</a>

    {{ include('annonces/_delete_form.html.twig') }}
{% endblock %}
